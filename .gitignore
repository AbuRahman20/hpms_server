node_modules




app.get('/fetchusers', async (req, res) => {
    const users = await UserModel.find()
    res.json(users)
})

app.post('/input', async (req, res) => {
    console.log(req.body)
    const { regno, student, parents } = req.body
    const newUser = new UserModel({ name: regno, email: student, phone: parents })
    await newUser.save()
})